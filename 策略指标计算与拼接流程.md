# 策略指标计算与拼接到 UserPrompt 的完整流程

## 概览

当用户在策略设置中配置技术指标（如 EMA、MACD、RSI、ATR、BOLL）和 K 线数量后，系统会按照以下流程计算指标并拼接到 UserPrompt 中传递给 AI。

---

## 完整流程图

```
用户策略配置 (store/strategy.go)
    ↓
    ├─ StrategyConfig.Indicators.EnableEMA = true
    ├─ StrategyConfig.Indicators.EMAPeriods = [20, 50]
    ├─ StrategyConfig.Indicators.EnableRSI = true
    ├─ StrategyConfig.Indicators.RSIPeriods = [7, 14]
    ├─ StrategyConfig.Indicators.Klines.PrimaryCount = 100
    └─ StrategyConfig.Indicators.Klines.SelectedTimeframes = ["30m", "4h"]
    ↓
策略引擎初始化 (kernel/engine.go)
    ↓
    NewStrategyEngine(config) → StrategyEngine
    ↓
交易员运行周期 (trader/auto_trader.go)
    ↓
    runCycle() → buildTradingContext()
    ↓
获取候选币 (kernel/engine.go)
    ↓
    strategyEngine.GetCandidateCoins()
    ├─ 根据 CoinSource 配置获取币种列表
    └─ 返回 []CandidateCoin
    ↓
获取市场数据 (kernel/engine.go)
    ↓
    fetchMarketDataWithStrategy(ctx, engine)
    ├─ 读取策略配置：
    │   ├─ timeframes = config.Indicators.Klines.SelectedTimeframes  // ["30m", "4h"]
    │   ├─ primaryTimeframe = config.Indicators.Klines.PrimaryTimeframe  // "30m"
    │   └─ klineCount = config.Indicators.Klines.PrimaryCount  // 100
    │
    ├─ 对每个币种调用：
    │   market.GetWithTimeframes(symbol, timeframes, primaryTimeframe, klineCount)
    │
    └─ 存储到 ctx.MarketDataMap[symbol]
    ↓
市场数据获取 (market/data.go)
    ↓
    GetWithTimeframes(symbol, ["30m", "4h"], "30m", 100)
    ├─ 获取各时间周期的 K 线数据
    │   ├─ getKlines(symbol, "30m", 200)  // 获取原始 K 线
    │   └─ getKlines(symbol, "4h", 200)
    │
    ├─ 计算时间序列指标：
    │   calculateTimeframeSeriesWithConfig(klines, "30m", 100, emaPeriods, rsiPeriods, atrPeriods)
    │   │
    │   ├─ 从策略配置读取周期参数：
    │   │   ├─ emaPeriods = [20, 50]  // 从 config.Indicators.EMAPeriods
    │   │   ├─ rsiPeriods = [7, 14]   // 从 config.Indicators.RSIPeriods
    │   │   └─ atrPeriods = [14]      // 从 config.Indicators.ATRPeriods
    │   │
    │   ├─ 取最新 100 根 K 线（count 参数）
    │   │
    │   ├─ 对每根 K 线计算指标：
    │   │   ├─ 如果 EnableEMA：
    │   │   │   ├─ calculateEMA(klines[:i+1], 20) → EMAValues[20][i]
    │   │   │   └─ calculateEMA(klines[:i+1], 50) → EMAValues[50][i]
    │   │   │
    │   │   ├─ 如果 EnableMACD：
    │   │   │   └─ calculateMACD(klines[:i+1]) → MACDValues[i]
    │   │   │
    │   │   ├─ 如果 EnableRSI：
    │   │   │   ├─ calculateRSI(klines[:i+1], 7) → RSIValues[7][i]
    │   │   │   └─ calculateRSI(klines[:i+1], 14) → RSIValues[14][i]
    │   │   │
    │   │   ├─ 如果 EnableBOLL：
    │   │   │   └─ calculateBOLL(klines[:i+1], 20, 2.0) → BOLLUpper[i], BOLLMiddle[i], BOLLLower[i]
    │   │   │
    │   │   └─ 如果 EnableATR：
    │   │       └─ calculateATR(klines, 14) → ATRValues[14]
    │   │
    │   └─ 返回 TimeframeSeriesData{
    │         Timeframe: "30m",
    │         Klines: [100个OHLCV],
    │         EMAValues: {20: [100个值], 50: [100个值]},
    │         MACDValues: [100个值],
    │         RSIValues: {7: [100个值], 14: [100个值]},
    │         ATRValues: {14: 单个值},
    │         BOLLUpper/Middle/Lower: [100个值]
    │       }
    │
    ├─ 计算当前指标（基于主时间周期最新数据）：
    │   ├─ currentEMA20 = calculateEMA(primaryKlines, 20)
    │   ├─ currentMACD = calculateMACD(primaryKlines)
    │   └─ currentRSI7 = calculateRSI(primaryKlines, 7)
    │
    └─ 返回 Data{
          Symbol: "BTCUSDT",
          CurrentPrice: 95000.0,
          CurrentEMA20: 94500.0,
          CurrentMACD: 150.5,
          CurrentRSI7: 65.3,
          TimeframeData: {
            "30m": TimeframeSeriesData{...},
            "4h": TimeframeSeriesData{...}
          }
        }
    ↓
构建 UserPrompt (kernel/engine.go)
    ↓
    engine.BuildUserPrompt(ctx)
    ├─ 读取策略配置的指标开关：
    │   indicators = e.config.Indicators
    │
    ├─ 格式化市场数据：
    │   formatMarketData(data)
    │   │
    │   ├─ 输出当前指标（如果启用）：
    │   │   "current_price = 95000.0"
    │   │   + (if EnableEMA) ", current_ema20 = 94500.0"
    │   │   + (if EnableMACD) ", current_macd = 150.5"
    │   │   + (if EnableRSI) ", current_rsi7 = 65.3"
    │   │
    │   └─ 对每个时间周期调用：
    │       formatTimeframeSeriesData(sb, tfData, indicators)
    │       │
    │       ├─ 输出 OHLCV K 线表格（100 行）：
    │       │   "Time(UTC)      Open      High      Low       Close     Volume"
    │       │   "01-18 10:00   95000.0   95200.0   94800.0   95100.0   1234567.89"
    │       │   "01-18 10:30   95100.0   95300.0   94900.0   95200.0   2345678.90"
    │       │   ... (共 100 行)
    │       │
    │       ├─ 如果 EnableEMA：
    │       │   遍历 EMAValues 的所有周期：
    │       │   "EMA20: [94500.0, 94520.0, 94540.0, ..., 94800.0]"  // 100个值
    │       │   "EMA50: [94300.0, 94310.0, 94320.0, ..., 94600.0]"  // 100个值
    │       │
    │       ├─ 如果 EnableMACD：
    │       │   "MACD: [145.2, 146.8, 148.3, ..., 150.5]"  // 100个值
    │       │
    │       ├─ 如果 EnableRSI：
    │       │   遍历 RSIValues 的所有周期：
    │       │   "RSI7: [62.3, 63.1, 64.5, ..., 65.3]"   // 100个值
    │       │   "RSI14: [58.7, 59.2, 60.1, ..., 61.8]"  // 100个值
    │       │
    │       ├─ 如果 EnableATR：
    │       │   遍历 ATRValues 的所有周期：
    │       │   "ATR14: 234.56"  // 单个值
    │       │
    │       └─ 如果 EnableBOLL：
    │           "BOLL Upper: [95800.0, 95850.0, ..., 96000.0]"   // 100个值
    │           "BOLL Middle: [95000.0, 95020.0, ..., 95200.0]"  // 100个值
    │           "BOLL Lower: [94200.0, 94190.0, ..., 94400.0]"   // 100个值
    │
    └─ 拼接完整 UserPrompt：
        """
        Time: 2026-01-18 12:00:00 | Period: #42 | Runtime: 126 minutes

        BTC: 95000.00 (1h: +1.20%, 4h: +3.50%) | MACD: 150.4 | RSI: 65.2

        Account: Equity 10000.00 | Balance 8500.00 (85.0%) | PnL +5.20% | Margin 15.0% | Positions 2

        ## Current Positions
        1. ETHUSDT LONG | Entry 3500.0 Current 3550.0 | ...
        
        === ETHUSDT Market Data ===
        current_price = 3550.0, current_ema20 = 3520.0, current_macd = 12.5, current_rsi7 = 68.3

        === 30M Timeframe (oldest → latest) ===
        Time(UTC)      Open      High      Low       Close     Volume
        01-18 08:00   3500.0    3520.0    3495.0    3510.0    123456.78
        01-18 08:30   3510.0    3530.0    3505.0    3525.0    234567.89
        ... (共 100 行)

        EMA20: [3480.0, 3485.0, 3490.0, ..., 3520.0]
        EMA50: [3450.0, 3455.0, 3460.0, ..., 3490.0]
        MACD: [10.2, 10.5, 10.8, ..., 12.5]
        RSI7: [65.0, 66.2, 67.1, ..., 68.3]
        RSI14: [62.5, 63.0, 63.8, ..., 65.2]
        ATR14: 45.67
        BOLL Upper: [3600.0, 3605.0, ..., 3620.0]
        BOLL Middle: [3500.0, 3505.0, ..., 3520.0]
        BOLL Lower: [3400.0, 3405.0, ..., 3420.0]

        === 4H Timeframe (oldest → latest) ===
        ... (同样格式，100 根 K 线 + 指标)

        ## Candidate Coins (5 coins)
        ### 1. BTCUSDT (AI500)
        ... (同样格式的市场数据)

        ---
        Now please analyze and output your decision (Chain of Thought + JSON)
        """
    ↓
传递给 AI (kernel/engine.go)
    ↓
    GetFullDecisionWithStrategy(ctx, mcpClient, engine, variant)
    ├─ systemPrompt = engine.BuildSystemPrompt(...)
    ├─ userPrompt = engine.BuildUserPrompt(ctx)  // 上面构建的完整提示词
    └─ mcpClient.CallWithMessages(systemPrompt, userPrompt)
    ↓
AI 分析并返回决策
```

---

## 关键代码位置

### 1. 策略配置定义
**文件**: `store/strategy.go`

```go
type IndicatorConfig struct {
    Klines KlineConfig `json:"klines"`
    
    // 技术指标开关
    EnableEMA  bool `json:"enable_ema"`
    EnableMACD bool `json:"enable_macd"`
    EnableRSI  bool `json:"enable_rsi"`
    EnableATR  bool `json:"enable_atr"`
    EnableBOLL bool `json:"enable_boll"`
    
    // 指标周期配置
    EMAPeriods  []int `json:"ema_periods,omitempty"`  // [20, 50]
    RSIPeriods  []int `json:"rsi_periods,omitempty"`  // [7, 14]
    ATRPeriods  []int `json:"atr_periods,omitempty"`  // [14]
    BOLLPeriods []int `json:"boll_periods,omitempty"` // [20]
}

type KlineConfig struct {
    PrimaryTimeframe   string   `json:"primary_timeframe"`    // "30m"
    PrimaryCount       int      `json:"primary_count"`        // 100
    SelectedTimeframes []string `json:"selected_timeframes"`  // ["30m", "4h"]
}
```

### 2. 市场数据获取
**文件**: `kernel/engine.go` → `fetchMarketDataWithStrategy()`

```go
func fetchMarketDataWithStrategy(ctx *Context, engine *StrategyEngine) error {
    config := engine.GetConfig()
    
    // 从策略配置读取参数
    timeframes := config.Indicators.Klines.SelectedTimeframes  // ["30m", "4h"]
    primaryTimeframe := config.Indicators.Klines.PrimaryTimeframe  // "30m"
    klineCount := config.Indicators.Klines.PrimaryCount  // 100
    
    // 对每个币种获取市场数据
    for _, coin := range ctx.CandidateCoins {
        data, err := market.GetWithTimeframes(
            coin.Symbol, 
            timeframes,      // ["30m", "4h"]
            primaryTimeframe, // "30m"
            klineCount       // 100
        )
        ctx.MarketDataMap[coin.Symbol] = data
    }
}
```

### 3. 指标计算
**文件**: `market/data.go` → `calculateTimeframeSeriesWithConfig()`

```go
func calculateTimeframeSeriesWithConfig(
    klines []Kline, 
    timeframe string, 
    count int,           // 100 (从策略配置)
    emaPeriods []int,    // [20, 50] (从策略配置)
    rsiPeriods []int,    // [7, 14] (从策略配置)
    atrPeriods []int     // [14] (从策略配置)
) *TimeframeSeriesData {
    
    // 取最新 count 根 K 线
    start := len(klines) - count  // 取最新 100 根
    
    // 对每根 K 线计算指标
    for i := start; i < len(klines); i++ {
        // 计算 EMA（每个周期）
        for _, period := range emaPeriods {  // [20, 50]
            if i >= period-1 {
                ema := calculateEMA(klines[:i+1], period)
                data.EMAValues[period] = append(data.EMAValues[period], ema)
            }
        }
        
        // 计算 MACD
        if i >= 25 {
            macd := calculateMACD(klines[:i+1])
            data.MACDValues = append(data.MACDValues, macd)
        }
        
        // 计算 RSI（每个周期）
        for _, period := range rsiPeriods {  // [7, 14]
            if i >= period {
                rsi := calculateRSI(klines[:i+1], period)
                data.RSIValues[period] = append(data.RSIValues[period], rsi)
            }
        }
        
        // 计算 BOLL
        if i >= 19 {
            upper, middle, lower := calculateBOLL(klines[:i+1], 20, 2.0)
            data.BOLLUpper = append(data.BOLLUpper, upper)
            data.BOLLMiddle = append(data.BOLLMiddle, middle)
            data.BOLLLower = append(data.BOLLLower, lower)
        }
    }
    
    // 计算 ATR（每个周期）
    for _, period := range atrPeriods {  // [14]
        atr := calculateATR(klines, period)
        data.ATRValues[period] = atr
    }
    
    return data
}
```

### 4. 指标拼接到 UserPrompt
**文件**: `kernel/engine.go` → `formatTimeframeSeriesData()`

```go
func (e *StrategyEngine) formatTimeframeSeriesData(
    sb *strings.Builder, 
    data *market.TimeframeSeriesData, 
    indicators store.IndicatorConfig
) {
    // 输出 OHLCV K 线表格
    if len(data.Klines) > 0 {
        sb.WriteString("Time(UTC)      Open      High      Low       Close     Volume\n")
        for i, k := range data.Klines {  // 100 根 K 线
            t := time.Unix(k.Time/1000, 0).UTC()
            sb.WriteString(fmt.Sprintf("%-14s %-9.4f %-9.4f %-9.4f %-9.4f %-12.2f\n",
                t.Format("01-02 15:04"), k.Open, k.High, k.Low, k.Close, k.Volume))
        }
    }
    
    // 如果启用 EMA
    if indicators.EnableEMA {
        // 遍历所有 EMA 周期
        for period, values := range data.EMAValues {  // {20: [100个值], 50: [100个值]}
            if len(values) > 0 {
                sb.WriteString(fmt.Sprintf("EMA%d: %s\n", period, formatFloatSlice(values)))
                // 输出: "EMA20: [94500.0, 94520.0, ..., 94800.0]"
            }
        }
    }
    
    // 如果启用 MACD
    if indicators.EnableMACD && len(data.MACDValues) > 0 {
        sb.WriteString(fmt.Sprintf("MACD: %s\n", formatFloatSlice(data.MACDValues)))
        // 输出: "MACD: [145.2, 146.8, ..., 150.5]"
    }
    
    // 如果启用 RSI
    if indicators.EnableRSI {
        // 遍历所有 RSI 周期
        for period, values := range data.RSIValues {  // {7: [100个值], 14: [100个值]}
            if len(values) > 0 {
                sb.WriteString(fmt.Sprintf("RSI%d: %s\n", period, formatFloatSlice(values)))
                // 输出: "RSI7: [62.3, 63.1, ..., 65.3]"
            }
        }
    }
    
    // 如果启用 ATR
    if indicators.EnableATR {
        // 遍历所有 ATR 周期
        for period, value := range data.ATRValues {  // {14: 234.56}
            if value > 0 {
                sb.WriteString(fmt.Sprintf("ATR%d: %.4f\n", period, value))
                // 输出: "ATR14: 234.56"
            }
        }
    }
    
    // 如果启用 BOLL
    if indicators.EnableBOLL && len(data.BOLLUpper) > 0 {
        sb.WriteString(fmt.Sprintf("BOLL Upper: %s\n", formatFloatSlice(data.BOLLUpper)))
        sb.WriteString(fmt.Sprintf("BOLL Middle: %s\n", formatFloatSlice(data.BOLLMiddle)))
        sb.WriteString(fmt.Sprintf("BOLL Lower: %s\n", formatFloatSlice(data.BOLLLower)))
        // 输出: "BOLL Upper: [95800.0, 95850.0, ..., 96000.0]"
    }
}
```

---

## 数据流转示例

假设用户配置：
- **时间周期**: ["30m", "4h"]
- **主时间周期**: "30m"
- **K 线数量**: 100
- **启用指标**: EMA(20, 50), RSI(7, 14), MACD, ATR(14)

### 步骤 1: 读取策略配置
```go
config.Indicators.Klines.SelectedTimeframes = ["30m", "4h"]
config.Indicators.Klines.PrimaryTimeframe = "30m"
config.Indicators.Klines.PrimaryCount = 100
config.Indicators.EnableEMA = true
config.Indicators.EMAPeriods = [20, 50]
config.Indicators.EnableRSI = true
config.Indicators.RSIPeriods = [7, 14]
config.Indicators.EnableMACD = true
config.Indicators.EnableATR = true
config.Indicators.ATRPeriods = [14]
```

### 步骤 2: 获取 K 线数据
```go
// 获取 30m 时间周期的 200 根 K 线（用于计算指标）
klines30m = getKlines("BTCUSDT", "30m", 200)  // 返回 200 根 K 线

// 获取 4h 时间周期的 200 根 K 线
klines4h = getKlines("BTCUSDT", "4h", 200)
```

### 步骤 3: 计算 30m 时间周期指标
```go
// 取最新 100 根 K 线
start = 200 - 100 = 100
klines = klines30m[100:200]  // 最新 100 根

// 对每根 K 线计算指标
for i = 100 to 199:
    // EMA20
    ema20 = calculateEMA(klines30m[0:i+1], 20)
    EMAValues[20].append(ema20)  // 100 个值
    
    // EMA50
    ema50 = calculateEMA(klines30m[0:i+1], 50)
    EMAValues[50].append(ema50)  // 100 个值
    
    // MACD
    macd = calculateMACD(klines30m[0:i+1])
    MACDValues.append(macd)  // 100 个值
    
    // RSI7
    rsi7 = calculateRSI(klines30m[0:i+1], 7)
    RSIValues[7].append(rsi7)  // 100 个值
    
    // RSI14
    rsi14 = calculateRSI(klines30m[0:i+1], 14)
    RSIValues[14].append(rsi14)  // 100 个值

// ATR14 (单个值)
atr14 = calculateATR(klines30m, 14)
ATRValues[14] = atr14
```

### 步骤 4: 拼接到 UserPrompt
```
=== 30M Timeframe (oldest → latest) ===

Time(UTC)      Open      High      Low       Close     Volume
01-18 08:00   95000.0   95200.0   94800.0   95100.0   1234567.89
01-18 08:30   95100.0   95300.0   94900.0   95200.0   2345678.90
01-18 09:00   95200.0   95400.0   95000.0   95300.0   3456789.01
... (共 100 行)

EMA20: [94500.0, 94520.0, 94540.0, 94560.0, ..., 94800.0]  (100个值)
EMA50: [94300.0, 94310.0, 94320.0, 94330.0, ..., 94600.0]  (100个值)
MACD: [145.2, 146.8, 148.3, 149.1, ..., 150.5]  (100个值)
RSI7: [62.3, 63.1, 64.5, 65.2, ..., 65.3]  (100个值)
RSI14: [58.7, 59.2, 60.1, 60.8, ..., 61.8]  (100个值)
ATR14: 234.56  (单个值)

=== 4H Timeframe (oldest → latest) ===
... (同样格式，100 根 K 线 + 指标)
```

---

## 总结

1. **策略配置** (`store/strategy.go`): 用户设置指标开关、周期参数、K 线数量
2. **策略引擎** (`kernel/engine.go`): 读取配置并协调数据获取
3. **市场数据获取** (`market/data.go`): 根据配置参数获取 K 线并计算指标
4. **指标计算** (`market/data.go`): 使用配置的周期参数计算各指标的时间序列
5. **格式化输出** (`kernel/engine.go`): 根据指标开关将计算结果拼接到 UserPrompt
6. **传递给 AI**: 完整的 UserPrompt 包含所有启用的指标数据

整个流程完全由策略配置驱动，用户在前端设置的所有参数都会被正确读取、计算并传递给 AI。
