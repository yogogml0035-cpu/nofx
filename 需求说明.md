# 需求文档：回测策略集成

## 简介

本功能旨在将回测模块与策略配置深度集成，确保回测和实盘交易使用相同的策略逻辑、指标计算和 UserPrompt 生成方式。当前回测模块存在独立的 UserPrompt 生成逻辑，与实盘交易的逻辑分离，导致回测结果可能与实盘表现不一致。通过本功能，回测将完全复用策略引擎的代码，确保一致性和可维护性。

## 术语表

- **回测模块 (Backtest Module)**: 系统中用于模拟历史交易的模块，位于 `backtest/` 目录
- **策略引擎 (Strategy Engine)**: 核心策略执行引擎，位于 `kernel/engine.go`，负责生成 UserPrompt 和 SystemPrompt
- **策略配置 (Strategy Config)**: 用户定义的策略参数，包括指标开关、周期参数、K线配置等，存储在 `store/strategy.go`
- **UserPrompt**: 传递给 AI 的用户提示词，包含市场数据、持仓信息、候选币种等
- **SystemPrompt**: 传递给 AI 的系统提示词，定义 AI 的角色、风险控制规则等
- **历史数据 (Historical Data)**: 过去某个时间点的市场数据，用于回测
- **实盘交易 (Live Trading)**: 使用最新市场数据进行的真实交易
- **指标 (Indicator)**: 技术分析指标，如 EMA、MACD、RSI、ATR、BOLL 等
- **时间周期 (Timeframe)**: K线的时间间隔，如 5m、15m、1h、4h 等
- **主时间周期 (Primary Timeframe)**: 策略配置中指定的主要分析时间周期
- **K线数量 (Kline Count)**: 用于计算指标的 K线根数

## 需求

### 需求 1：策略配置读取

**用户故事**: 作为回测用户，我希望回测能够读取并使用我选择的策略配置，这样回测结果才能准确反映该策略的表现。

#### 验收标准

1. WHEN 用户在回测前端选择策略后，THE 回测后端 SHALL 读取该策略的完整配置
2. THE 回测后端 SHALL 读取策略配置中的指标开关（EnableEMA、EnableMACD、EnableRSI、EnableATR、EnableBOLL）
3. THE 回测后端 SHALL 读取策略配置中的指标周期参数（EMAPeriods、RSIPeriods、ATRPeriods、BOLLPeriods）
4. THE 回测后端 SHALL 读取策略配置中的 K线配置（SelectedTimeframes、PrimaryTimeframe、PrimaryCount）
5. THE 回测后端 SHALL 将策略配置转换为 StrategyEngine 可用的格式

### 需求 2：历史数据获取

**用户故事**: 作为回测用户，我希望回测能够获取指定时间点的历史数据，这样才能模拟真实的交易决策过程。

#### 验收标准

1. WHEN 回测需要某个决策点的数据时，THE 系统 SHALL 获取该时间点之前的历史 K线数据
2. THE 系统 SHALL 支持使用 CoinAnk API 的 ts 参数查询历史数据
3. THE 系统 SHALL 支持使用 OKX API 的 before/after 参数查询历史数据
4. THE 系统 SHALL 支持使用 GetKlinesRange() 函数获取指定时间范围的数据
5. THE 系统 SHALL 为每个决策点获取策略配置中指定的所有时间周期的历史数据
6. THE 系统 SHALL 确保历史数据的时间戳不超过当前回测时间点

### 需求 3：指标计算

**用户故事**: 作为回测用户，我希望回测使用与实盘相同的指标计算方法，这样回测结果才具有参考价值。

#### 验收标准

1. THE 回测 SHALL 使用 market.calculateTimeframeSeriesWithConfig() 函数计算指标
2. THE 回测 SHALL 根据策略配置的周期参数计算 EMA（如 [20, 50]）
3. THE 回测 SHALL 根据策略配置的周期参数计算 RSI（如 [7, 14]）
4. THE 回测 SHALL 根据策略配置的周期参数计算 ATR（如 [14]）
5. THE 回测 SHALL 根据策略配置的周期参数计算 BOLL（如 [20]）
6. THE 回测 SHALL 根据策略配置的指标开关决定是否计算 MACD
7. THE 回测 SHALL 计算策略配置中指定的 K线数量（如 100 根）的指标数据
8. THE 回测 SHALL 确保指标计算使用的数据不超过当前回测时间点

### 需求 4：统一 UserPrompt 生成

**用户故事**: 作为回测用户，我希望回测和实盘使用完全相同的 UserPrompt 格式，这样 AI 的行为才能保持一致。

#### 验收标准

1. THE 回测 SHALL 创建 StrategyEngine 实例并传入策略配置
2. THE 回测 SHALL 使用 StrategyEngine.BuildUserPrompt() 方法生成 UserPrompt
3. THE 回测 SHALL 使用 StrategyEngine.BuildSystemPrompt() 方法生成 SystemPrompt
4. THE 回测 SHALL 确保 UserPrompt 包含所有启用的指标数据
5. THE 回测 SHALL 确保 UserPrompt 的格式与实盘完全一致
6. THE 回测 SHALL 使用 formatTimeframeSeriesData() 格式化多时间周期数据
7. THE 回测 SHALL 根据策略配置的指标开关决定 UserPrompt 中包含哪些指标

### 需求 5：回测上下文扩展

**用户故事**: 作为开发者，我希望回测上下文能够支持历史数据模式，这样可以清晰地区分回测和实盘的数据获取方式。

#### 验收标准

1. THE 系统 SHALL 在 kernel.Context 中添加 HistoricalMode 字段（布尔类型）
2. THE 系统 SHALL 在 kernel.Context 中添加 StartTime 字段（可选时间类型）
3. THE 系统 SHALL 在 kernel.Context 中添加 EndTime 字段（可选时间类型）
4. WHEN HistoricalMode 为 true 且 StartTime 和 EndTime 不为空时，THE 系统 SHALL 使用历史数据查询
5. WHEN HistoricalMode 为 false 或为空时，THE 系统 SHALL 使用最新数据查询
6. THE 系统 SHALL 确保历史数据模式不影响现有的实盘交易功能

### 需求 6：回测配置转换

**用户故事**: 作为开发者，我希望回测配置能够无缝转换为策略配置，这样可以复用策略引擎的所有功能。

#### 验收标准

1. THE 系统 SHALL 提供 BacktestConfig.ToStrategyConfig() 方法
2. THE 方法 SHALL 将回测配置中的策略 ID 转换为完整的策略配置
3. THE 方法 SHALL 从数据库读取策略配置（如果提供了策略 ID）
4. THE 方法 SHALL 支持使用默认策略配置（如果未提供策略 ID）
5. THE 方法 SHALL 保留回测配置中的杠杆设置（BTCETHLeverage、AltcoinLeverage）
6. THE 方法 SHALL 保留回测配置中的时间周期设置（Timeframes）

### 需求 7：候选币种获取

**用户故事**: 作为回测用户，我希望回测能够使用策略配置中定义的币种来源，这样可以测试不同的币种选择策略。

#### 验收标准

1. THE 回测 SHALL 使用 StrategyEngine.GetCandidateCoins() 方法获取候选币种
2. THE 方法 SHALL 根据策略配置的 CoinSource.SourceType 决定币种来源
3. THE 方法 SHALL 支持 "static" 类型（静态币种列表）
4. THE 方法 SHALL 支持 "ai500" 类型（AI500 币池）
5. THE 方法 SHALL 支持 "oi_top" 类型（持仓量增长榜）
6. THE 方法 SHALL 支持 "mixed" 类型（混合多个来源）
7. WHEN 策略引擎获取候选币种失败时，THE 回测 SHALL 回退到回测配置中的 Symbols 列表



### 需求 8：数据流程一致性

**用户故事**: 作为开发者，我希望回测和实盘的数据流程保持一致，这样可以减少维护成本。

#### 验收标准

1. THE 回测 SHALL 使用与实盘相同的 fetchMarketDataWithStrategy() 函数
2. THE 回测 SHALL 使用与实盘相同的 calculateTimeframeSeriesWithConfig() 函数
3. THE 回测 SHALL 使用与实盘相同的 formatTimeframeSeriesData() 函数
4. THE 回测 SHALL 使用与实盘相同的 GetFullDecisionWithStrategy() 函数
5. THE 回测 SHALL 确保数据获取、指标计算、格式化、AI 调用的流程与实盘一致
6. THE 回测 SHALL 仅在数据来源（历史 vs 最新）上与实盘有差异

### 需求 9：历史数据接口实现

**用户故事**: 作为开发者，我希望有统一的历史数据获取接口，这样可以简化回测的数据获取逻辑。

#### 验收标准

1. THE 系统 SHALL 提供 market.GetWithTimeframesRange() 函数
2. THE 函数 SHALL 接受 symbol、timeframes、primaryTimeframe、count、startTime、endTime 参数
3. THE 函数 SHALL 为每个时间周期调用 GetKlinesRange() 获取历史数据
4. THE 函数 SHALL 返回与 GetWithTimeframes() 相同格式的 market.Data 结构
5. THE 函数 SHALL 计算历史数据的指标（EMA、MACD、RSI、ATR、BOLL）
6. THE 函数 SHALL 确保返回的数据时间戳不超过 endTime


### 需求 10：排名数据支持

**用户故事**: 作为回测用户，我希望回测能够使用策略配置中启用的排名数据，这样可以测试基于市场热度的策略。

#### 验收标准

1. WHEN 策略配置启用 EnableOIRanking 时，THE 回测 SHALL 获取持仓量排名数据
2. WHEN 策略配置启用 EnableNetFlowRanking 时，THE 回测 SHALL 获取资金流排名数据
3. WHEN 策略配置启用 EnablePriceRanking 时，THE 回测 SHALL 获取价格涨跌排名数据
4. THE 回测 SHALL 使用 StrategyEngine 的相应方法获取排名数据
5. THE 回测 SHALL 将排名数据存储到 Context 的相应字段中
6. THE 回测 SHALL 在 UserPrompt 中包含排名数据（如果启用）
7. THE 回测 SHALL 注意排名数据使用的是历史数据，而非当前数据

